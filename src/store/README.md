# store/

Zustand stores wiring `game/` logic to React + Firebase: local game state,
online room sync (Realtime Database), auth/user session, and leaderboard
queries (Firestore). Implemented in Step 3.
