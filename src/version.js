// Bumped by hand with each PR (see Home.jsx) so a player can tell, just by
// looking at the home screen, exactly which deployed build they're on —
// GitHub Pages deploys are otherwise invisible, with no build id anywhere
// else in the app.
export const APP_VERSION = 'v0.22'
